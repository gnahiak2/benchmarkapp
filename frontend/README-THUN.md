# Frontend

Hey Thun,

This folder is mainly for the frontend side of te project

Feel free to structure the HTML/CSS however you think works best. The frontend will eventually connect to the TypeScript benchmark engine and Rust API, but you can use mock data while those are still being developed.

## Main areas

- `*.html` — pages
- `css/` — styling
- `assets/` — images and other assets

I'll handle the engine, backend, and deployment, so you can focus on making the frontend work well and look good.

We'll connect everything together once the other parts are ready.
